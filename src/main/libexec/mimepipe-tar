#!/bin/sh

if test $# -le 1; then
	echo
	tar -tvf "$1"
else
	tmpdir=$(mktemp -dp /run/user/$UID mimepipe-XXXXXXXXXX)
	tar -C "$tmpdir" -xf "$1" "$2"
	shift
	env -C "$tmpdir" mimepipe "$@"
	rm -rf "$tmpdir"
fi
